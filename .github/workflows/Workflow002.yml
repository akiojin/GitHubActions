on:
  workflow_dispatch:
    inputs:
      CONFIGURATION:
        description: '構成の選択'
        required: true
        default: Debug
        type: choice
        options:
        - Debug
        - Release
jobs:
  Build:
    runs-on: macos-latest
    steps:
      - run: echo ${{ inputs.CONFIGURATION }}
